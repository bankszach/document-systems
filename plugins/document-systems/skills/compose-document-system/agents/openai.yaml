interface:
  display_name: "Compose Document System"
  short_description: "Build a semantically gated document packet"
  default_prompt: "Use $compose-document-system to turn this document need into a content-addressed proposal packet with source-backed semantic invariants."
dependencies:
  tools:
    - type: "mcp"
      value: "documentSystems"
      description: "Deterministic document composition, semantic gating, packet, validation, and rendering tools."
      transport: "stdio"
policy:
  allow_implicit_invocation: true
